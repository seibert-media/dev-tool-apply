{
  "name": "editorconfig",
  "applySteps": [{
    "description": "install npm package",
    "commands": [
      "npm install --save eclint"
    ],
    "checks": {
      "npm module is installed": "npm ls eclint"
    }
  },{
    "description": "initial config",
    "commands": [
      "cp $DEV_TOOL_APPLY_MODULES/editorconfig/.editorconfig ./",
      "git add .editorconfig"
    ],
    "checks": {
      ".editorconfig exists": "ls .editorconfig"
    }
  },{
    "description": "config diff",
    "commands": [
      "cp $DEV_TOOL_APPLY_MODULES/editorconfig/.editorconfig ./",
      "git add -p .editorconfig"
    ],
    "checks": {
      "compare .editorconfig": "git diff --no-index -- .editorconfig $DEV_TOOL_APPLY_PATH/src/modules/editorconfig/.editorconfig"
    }
  },{
    "description": "npm script 'eclint' exists",
    "commands": [
      "$DEV_TOOL_APPLY_BIN/json -I -f package.json -e 'this.scripts.eclint=\"eclint check .\"'"
    ],
    "checks": {
      "run eclint script": "npm run eclint"
    }
  },{
    "description": "npm script 'eclint-fix' exists",
    "commands": [
      "$DEV_TOOL_APPLY_BIN/json -I -f package.json -e 'this.scripts[\"eclint-fix\"]=\"eclint fix .\"'"
    ],
    "checks": {
      "run eclint-fix script": "npm run eclint-fix"
    }
  }]
}
